# 解决 404 错误 - 完整步骤

## 问题原因
之前的部署版本使用了 `/mouse_test/` 路径，浏览器可能缓存了旧的 HTML 文件。

## ✅ 已解决的问题
1. ✅ Vite 配置已修改为 `base: '/'`
2. ✅ 已清理并重新构建
3. ✅ 新构建的资源路径正确：`/assets/...`
4. ✅ CNAME 文件已正确生成

## 🚀 部署步骤

### 1. 提交新的构建
```bash
cd /soft/mouse_test
git add .
git commit -m "Fix base path for custom domain"
git push
```

### 2. 等待 GitHub Actions 部署完成
访问：https://github.com/devieed/mouse_test/actions
等待部署成功（绿色勾）

### 3. 配置 GitHub Pages 自定义域名

访问：https://github.com/devieed/mouse_test/settings/pages

在 **Custom domain** 部分：
- 输入：`ed9e.net`
- 点击 **Save**
- 等待 DNS 检查通过
- 勾选 **Enforce HTTPS**

### 4. 配置域名 DNS（如果还没配置）

在你的域名服务商（阿里云、腾讯云等）添加以下记录：

#### 方式1：A 记录（推荐）
```
类型: A
主机记录: @
记录值: 185.199.108.153
TTL: 600

类型: A
主机记录: @
记录值: 185.199.109.153
TTL: 600

类型: A
主机记录: @
记录值: 185.199.110.153
TTL: 600

类型: A
主机记录: @
记录值: 185.199.111.153
TTL: 600
```

#### 方式2：CNAME 记录（用于 www 子域名）
```
类型: CNAME
主机记录: www
记录值: devieed.github.io
TTL: 600
```

### 5. 清理浏览器缓存

**重要！** 旧的 HTML 文件可能被浏览器缓存了：

**Chrome/Edge:**
1. 按 `Ctrl + Shift + Delete`
2. 选择"缓存的图片和文件"
3. 点击"清除数据"

**或者使用隐身模式测试:**
- Chrome: `Ctrl + Shift + N`
- Firefox: `Ctrl + Shift + P`

**或者硬刷新:**
- Windows: `Ctrl + F5` 或 `Ctrl + Shift + R`
- Mac: `Cmd + Shift + R`

### 6. 验证部署

访问以下 URL 确认：

1. **主页（自动检测语言）**
   ```
   https://ed9e.net/
   ```

2. **指定语言**
   ```
   https://ed9e.net/?lang=zh-cn
   https://ed9e.net/?lang=en
   https://ed9e.net/?lang=ja
   ```

3. **检查资源路径**
   - 打开浏览器开发者工具（F12）
   - 查看 Network 标签
   - 确认资源路径为：`https://ed9e.net/assets/...`
   - **不应该**有 `/mouse_test/` 路径

## 🔍 故障排查

### 如果还是 404 错误：

1. **确认 GitHub Pages 配置**
   - Source: `GitHub Actions`
   - Custom domain: `ed9e.net`
   - Enforce HTTPS: 已勾选

2. **检查 DNS 是否生效**
   ```bash
   nslookup ed9e.net
   # 应该返回 GitHub Pages 的 IP 地址之一
   ```

3. **检查 CNAME 文件**
   访问：https://github.com/devieed/mouse_test/blob/main/dist/CNAME
   应该包含：`ed9e.net`

4. **清理 GitHub Pages 缓存**
   - 删除自定义域名
   - 保存
   - 等待 1 分钟
   - 重新添加自定义域名
   - 保存

5. **检查部署日志**
   访问：https://github.com/devieed/mouse_test/actions
   查看最新的部署日志，确认没有错误

### 常见问题

#### Q: 为什么还是显示 /mouse_test/ 路径？
A: 浏览器缓存了旧的 HTML。清理缓存或使用隐身模式测试。

#### Q: DNS 多久生效？
A: 通常几分钟，最多 24-48 小时。

#### Q: 为什么显示 404 Not Found？
A: 
- GitHub Pages 自定义域名未配置
- DNS 未生效
- 浏览器缓存未清理

#### Q: HTTPS 证书错误？
A: 等待几分钟，GitHub 正在自动签发 Let's Encrypt 证书。

## 📋 完成检查清单

- [ ] 代码已推送到 GitHub
- [ ] GitHub Actions 部署成功
- [ ] GitHub Pages 配置了自定义域名 `ed9e.net`
- [ ] DNS 已配置（A 记录或 CNAME）
- [ ] DNS 已生效（nslookup 测试）
- [ ] 清理了浏览器缓存
- [ ] 可以访问 https://ed9e.net/
- [ ] 资源路径正确（无 /mouse_test/）
- [ ] 语言切换功能正常
- [ ] HTTPS 证书有效

## 🎯 预期结果

部署成功后：
- ✅ 主页：https://ed9e.net/
- ✅ 简体中文：https://ed9e.net/?lang=zh-cn
- ✅ 英文：https://ed9e.net/?lang=en
- ✅ 资源路径：https://ed9e.net/assets/index-xxx.js
- ✅ HTTPS 正常工作
- ✅ 无 404 错误

现在推送代码并按照步骤操作即可！

